# language: ruby
# rvm:
#   - 2.2
#   - jruby
#   - rbx-2

# env:
#   global:
#     - CI=true
#   matrix:
#     - TEST_DOC=false
#     - TEST_DOC=true


# env:
# - DB=postgres
# - SH=bash
# - PACKAGE_VERSION="1.0.*"


language: ruby
rvm:
  - "2.2.0"
env:
  - DB=postgres
script:
  - RAILS_ENV=test bundle exec rake db:migrate --trace
  - bundle exec rake db:test:prepare
  - bundle exec rspec spec/
before_script:
  - postgres -e 'create database gamebook_test'
bundler_args: --binstubs=./bundler_stubs
